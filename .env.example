# No server env vars needed — Supabase credentials are set directly in public/index.html.
# See the SUPABASE_URL and SUPABASE_ANON constants near the top of the <script> block.
# Use your project's ANON (public) key, NOT the service_role key.
